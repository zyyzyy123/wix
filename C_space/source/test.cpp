#include <stdio.h>
#include <stdlib.h>
#include <windows.h>
#include <iostream>
using namespace std;
int main(){
    FILE *fp=NULL;
    char Path[_MAX_PATH];
    // GetModuleFileNameA(NULL,Path,MAX_PATH);                                                                          //文件指针
    // strcat(Path,"/../../etc/Doc.txt");
    // puts(Path);
    // fp=fopen(Path,"r");
    // if(fp==NULL){
        // printf("err\n");
    // }
    // else{
        // printf("ok\n");
        // fclose(fp);
    // }
}